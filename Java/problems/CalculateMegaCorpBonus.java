package problems;

public class CalculateMegaCorpBonus {

    public static int[] calculateBonus(int[] linesWritten) {
        int numWorkers = linesWritten.length;
        int[] bonuses = new int[numWorkers];
        return bonuses;
    }
}
